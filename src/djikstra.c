#include "../inc/djikstra.h"
#include "../inc/ioStructs.h"
#include <stdlib.h>
OutputData* djikstra(InputData* inputData) {
    if (inputData == NULL) {
        return NULL;
    }
    // Implementação do algoritmo de Dijkstra aqui
    return NULL;
}